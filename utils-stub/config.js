// Local fallback configuration for `initialize-uu-app.js`.
//
// This module is used when `local-config.json` doesn't define `source` (Document Store artifact config).
// It intentionally mirrors the structure of artifact-loaded config.
module.exports = require("./config.json");
